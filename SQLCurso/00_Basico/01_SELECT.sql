-- Muestra un resultado.
SELECT * FROM nombreTabla; -- Muestra toda la tabla.
SELECT nombreColumna FROM nombreTabla; -- Muestra solo la columna elegida de la tabla.
-- Pueden seleccionarse varias columnas separandolas por coma.
