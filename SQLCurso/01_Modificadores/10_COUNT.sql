-- Cuenta el número de resultados (no nulos).
SELECT COUNT(*) FROM nombreTabla; // Cuenta las filas de la tabla.
SELECT COUNT(nombreColumna) FROM nombreTabla; // Cuenta las filas de la tabla donde el valor en la columna dada es diferente a nulo.
